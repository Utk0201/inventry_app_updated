<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit product</title>
</head>
<body>
    <form action="/products/<%=product._id%>?_method=put" method="post">
        <label for="pName">Product's name</label>
        <input id="pNmae" type="text" name="name" placeholder="Write product's name" value="<%=product.name%>">
        <label for="price"></label>
        <input id="price" type="number" name="price" placeholder="Type product's price" value="<%=product.price%>">
        <label for="category">Select category</label>
        <select name="category" id="category">
            <%for(let category of categories){%>
                <option value="<%=category%>" <%=category===product.category?'selected':''%>><%=category%></option>
            <%}%>
        </select>
        <button>Submit</button>
    </form>
    <a href="/products/<%=product._id%>">Cancel</a>
</body>
</html>